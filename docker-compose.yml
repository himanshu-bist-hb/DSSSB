services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: dsssb
      POSTGRES_PASSWORD: dsssb
      POSTGRES_DB: dsssb
    ports:
      - "5432:5432"
    volumes:
      - dsssb_pg_data:/var/lib/postgresql/data

volumes:
  dsssb_pg_data:
